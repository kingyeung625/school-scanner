# 香港小學選校器 - Streamlit 版本

這是香港小學選校器的 Python + Streamlit 版本，可以部署到 Streamlit Cloud 或本地運行。

## 功能特點

- 🔍 **多條件篩選**：區域、校網、辦學團體、資助類型、學生性別、宗教、教學語言等
- 🏷️ **標籤搜索**：通過學校特色標籤快速查找
- 📊 **學校比較**：最多比較 4 所學校
- 📱 **響應式設計**：適配各種設備
- 🌐 **雙語支持**：繁體中文 / 簡體中文

## 安裝和運行

### 1. 安裝依賴

```bash
pip install -r requirements.txt
```

### 2. 準備數據文件

確保 CSV 文件位於正確位置：
```
streamlit_app/
├── app.py
├── requirements.txt
└── ... (其他文件)

../attached_assets/
└── database_school_info_1763020452726.csv
```

### 3. 運行應用

```bash
streamlit run app.py
```

應用將在 `http://localhost:8501` 運行。

## 部署到 Streamlit Cloud

1. 將代碼推送到 GitHub
2. 訪問 https://streamlit.io/cloud
3. 點擊 "New app"
4. 選擇你的 GitHub 倉庫和分支
5. 設置主文件路徑為 `streamlit_app/app.py`
6. 點擊 "Deploy"

## 項目結構

```
streamlit_app/
├── app.py                 # 主應用文件
├── requirements.txt       # Python 依賴
├── README.md             # 說明文檔
└── utils/                # 工具模塊
    ├── __init__.py
    ├── csv_parser.py     # CSV 解析
    ├── filters.py        # 篩選邏輯
    ├── sorting.py        # 排序邏輯
    └── i18n.py           # 雙語支持
```

## 注意事項

- 確保 CSV 文件路徑正確
- 首次運行時數據加載可能需要一些時間
- 建議使用 Python 3.8 或更高版本


